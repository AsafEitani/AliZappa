extends ../layouts/layout_noheader
block layout-head
  style.
    .sidebar {
    margin: 0;
    padding: 0;
    width: 200px;
    background-color: #f1f1f1;
    position: fixed;
    height: 100%;
    overflow: auto;
    }
    /* Sidebar links */
    .sidebar a {
    display: block;
    color: black;
    padding: 16px;
    text-decoration: none;
    }
    /* Active/current link */
    .sidebar a.active {
    background-color: #04AA6D;
    color: white;
    }
    /* Links on mouse-over */
    .sidebar a:hover:not(.active) {
    background-color: #555;
    color: white;
    }
    /* Page content. The value of the margin-left property should match the value of the sidebar's width property */
    div.content {
    margin-left: 200px;
    padding: 1px 16px;
    height: 1000px;
    }
    /* On screens that are less than 700px wide, make the sidebar into a topbar */
    media screen and (max-width: 700px) {
    .sidebar {
    width: 100%;
    height: auto;
    position: relative;
    }
    .sidebar a {float: left;}
    div.content {margin-left: 0;}
    }
    /* On screens that are less than 400px, display the bar vertically, instead of horizontally */
    media screen and (max-width: 400px) {
    .sidebar a {
    text-align: center;
    float: none;
    }
    }

block layout-content
  .graph-container
    aside.sidebar
      a#sup Manage Suppliers
      a#prod.1btn.1btn-primary Manage Products
      a#cust.1btn.1btn-primary Manage Customers
      a#stat.1btn.1btn-primary Statistics - By Address
      a#stat2.1btn.1btn-primary Statistics - By Products
      a#stat2.1btn.1btn-primary(href='/home') Home
    #maindiv.content

  script.
    $("#sup").click(test1)
    $("#prod").click(test2)
    $("#cust").click(test3)
    $("#stat").click(test4)
    $("#stat2").click(test5)
    $(".container").css('margin-left', '0px');
  
    function test1() {
        $.get('/suppliers/index', {}, function(data, status) {
            $("#maindiv").html(data);
            $("#maindiv").html(data);
            $("#maindiv").html(data);
            $("#maindiv").html(data);
        })
    }
  
    function test2() {
        $.get('/products/prodadmin', {}, function(data, status) {
            $("#maindiv").html(data);
        })
    }
  
    function test3() {
        $.get('/customers/index', {}, function(data, status) {
            $("#maindiv").html(data);
        })
    }
  
    function test4() {
        var token = $("#hid").html();
        $.get('/statistics/index', {}, function(data, status) {
            $("#maindiv").html(data);
        })
    }
  
    function test5() {
        $.get('/statistics/ByProduct', {}, function(data, status) {
            $("#maindiv").html(data);
        })
    } 
    
